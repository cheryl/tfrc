;; /loaded ~/tiny.bind.tf
;
;
; no F1
/def -b'OQ' = x %; hp %; gl
/def -b'OR' = viewguard
/def -b'OS' = /ps %; b

/def -b'[15~' = mt
/def -b'[17~' = ft
/def -b'[18~' = 1l
/def -b'[19~' = cd

/def -b'[20~' = gboh %; sa %; cheavy
/def -b'[21~' = sga
/def -b'[23~' = wealth %; lev
/def -b'[24~' = dw %; users
; for env, f4 is fsee
; for silme, f4 is kt

/def -b'[1;5A' = north
/def -b'[1;5D' = west
/def -b'[1;5B' = south
/def -b'[1;5C' = east
/def -b'[1;2A' = northeast
/def -b'[1;2D' = northwest
/def -b'[1;2B' = southwest
/def -b'[1;2C' = southeast

;Home	/dokey_home		cursor to beginning of line
;PgDn	/dokey page		scroll 1 screenful at --More--
;
;KP1 (upper left), KP2 (upper right), KP3 (center), KP4 (lower left), KP5 (lower right) prompt
